spring:
  application:
    name: letterboxd-api
  datasource:
    url: jdbc:postgresql://localhost:5432/letterboxd
    username: kennedy
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080